general {
    ignore_dbus_inhibit = false
}

# dpms
listener {
    timeout = 900
    on-timeout = hyprlock & brightnessctl set -d asus::kbd_backlight 0 && hyprctl dispatch dpms off
    on-resume = brightnessctl set -d asus::kbd_backlight 1 && hyprctl dispatch dpms on
}

# Suspend
listener {
    timeout = 3600
    on-timeout = systemctl suspend
}

